# Chrome DevTools MCP Reference

This directory holds small upstream reference snippets from the Chrome DevTools MCP project.

The extension runtime does not import these files directly. They are kept as implementation references for locally adapted code, especially `background/control/snapshot/formatter.js`.

Upstream files retain their original license headers.
